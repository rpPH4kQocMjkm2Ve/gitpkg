# /etc/gitpkg/gitpkg.conf
# gitpkg configuration
#
# Values can be overridden with command-line flags.
# Lines starting with # are ignored.

# Clone timeout in seconds
#CLONE_TIMEOUT=120

# Fetch timeout in seconds
#FETCH_TIMEOUT=30

# Status check timeout in seconds
#STATUS_TIMEOUT=15
